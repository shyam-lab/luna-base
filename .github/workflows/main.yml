name: lunabase CI

on:
  push:
    branches: master
  pull_request:
    branches: master

jobs:
  win_build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install fftw
      run: |
        pacman -S mingw-w64-x86_64-fftw
    - name: Make
      run: |
        make ARCH=WINDOWS

